/**
 * Below are the colors that are used in the app. The colors are defined in the light and dark mode.
 * There are many other ways to style your app. For example, [Nativewind](https://www.nativewind.dev/), [Tamagui](https://tamagui.dev/), [unistyles](https://reactnativeunistyles.vercel.app), etc.
 */

import '@/global.css';

import { Platform } from 'react-native';

export const Colors = {
  light: {
    text: '#1C1E21', // Dark grey text
    background: '#F0F2F5', // Light grey background
    backgroundElement: '#FFFFFF', // White element
    backgroundSelected: '#E0E3E8', // Light grey selected
    textSecondary: '#60646C',
    primary: '#FF6D00', // Energetic Orange
    secondary: '#0052CC', // Deep Blue
  },
  dark: {
    text: '#FFFFFF', // White text
    background: '#121212', // Very dark grey (almost black)
    backgroundElement: '#1E1E1E', // Dark grey element
    backgroundSelected: '#2E2E2E', // Slightly lighter grey selected
    textSecondary: '#9BA1A6',
    primary: '#FF8A33', // Slightly lighter Orange for dark mode
    secondary: '#3388FF', // Lighter Blue for dark mode
  },
} as const;

export type ThemeColor = keyof typeof Colors.light & keyof typeof Colors.dark;

export const Fonts = Platform.select({
  ios: {
    /** iOS `UIFontDescriptorSystemDesignDefault` */
    sans: 'system-ui',
    /** iOS `UIFontDescriptorSystemDesignSerif` */
    serif: 'ui-serif',
    /** iOS `UIFontDescriptorSystemDesignRounded` */
    rounded: 'ui-rounded',
    /** iOS `UIFontDescriptorSystemDesignMonospaced` */
    mono: 'ui-monospace',
  },
  default: {
    sans: 'normal',
    serif: 'serif',
    rounded: 'normal',
    mono: 'monospace',
  },
  web: {
    sans: 'var(--font-display)',
    serif: 'var(--font-serif)',
    rounded: 'var(--font-rounded)',
    mono: 'var(--font-mono)',
  },
});

export const Spacing = {
  half: 2,
  one: 4,
  two: 8,
  three: 16,
  four: 24,
  five: 32,
  six: 64,
} as const;

export const BottomTabInset = Platform.select({ ios: 50, android: 80 }) ?? 0;
export const MaxContentWidth = 800;
